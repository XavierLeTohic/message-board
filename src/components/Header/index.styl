@import '../../styles/variables.styl'

header
  top 0
  position sticky
  padding $spacer
  margin-bottom $spacer
  font-size $title-size
  border-top $spacer-s solid $dark-primary
  color $light-text
  background-color $primary